# Synthetic example provenance

The CSV and two one-page PDFs are original synthetic Motifuse examples prepared
2026-09-04. No customer data or third-party dataset is included. The PDFs say
“Owned SDK example” and change a pressure limit from 8 bar to
10 bar. They use the built-in PDF Helvetica font without embedding a font file.

These fixture files are CC0-1.0. Example code follows the SDK's MIT licence.
They are not engineering instructions or proof of extraction accuracy. The
fixture pairs demonstrate a workflow; scan/extraction/comparison behavior still
depends on the actual deployed services and entitlements.

The fixture author dedicates these original CSV/PDF fixture files to the public domain under [CC0 1.0 Universal](https://creativecommons.org/publicdomain/zero/1.0/). Recreate the PDFs with `node scripts/create-example-fixtures.mjs`.
